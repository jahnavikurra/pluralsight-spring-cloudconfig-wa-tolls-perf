Our team confirmed that package-lock.json is intentionally not checked into this repository
and is generated during the build process.

Please analyze the existing pipeline/Docker build and determine:

1. Where and how package-lock.json is currently generated.
2. Whether the repository's existing CI uses npm install or npm ci.
3. Whether my new frontend_quality_checks job can use npm ci when package-lock.json is not committed.
4. If npm ci will fail, recommend the smallest change to make PR0 follow the repository's existing dependency-install convention.
5. Do not modify anything yet.

Also confirm whether package-lock.json should be removed from my feature branch based on the repository's existing convention.

Stop after the analysis.
