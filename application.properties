Before I commit PR0, perform one final verification only. Do not modify or fix application source code.

1. Run:
   git status
   git diff --stat
   git diff -- package.json
   git diff -- package-lock.json
   git diff -- frontend-idp-containerapp-pipeline.yml
   git diff -- eslint.config.mjs
   git diff -- .prettierrc.json
   git diff -- .prettierignore
   git diff --check

2. Confirm package-lock.json is updated and tracked because new devDependencies were added.

3. Confirm the ONLY intended changed/created files are:
   - package.json
   - package-lock.json
   - eslint.config.mjs
   - .prettierrc.json
   - .prettierignore
   - frontend-idp-containerapp-pipeline.yml

4. Confirm no files under src/ were changed.

5. Confirm these files were NOT changed:
   - next.config.js
   - next.config.ts
   - Dockerfile
   - tsconfig.json

6. Confirm existing build/deploy jobs have no new dependency on frontend_quality_checks.

7. Confirm frontend_quality_checks is non-blocking even when:
   - npm ci fails
   - typecheck fails
   - lint fails
   - format:check fails

8. Run final validation:
   npm run build:local

Do not fix the 53 TypeScript errors, ESLint findings, or Prettier mismatches.
Those are the baseline technical debt for later PRs.

Give me PASS/FAIL for each item and stop.
