Implement the smallest targeted fix based on the trace you just completed.

Do NOT change:
- NS_ID_DELIVERY_PLANS
- line 2389
- _MANAGE_DELIVERY_PLANS_ALLOW_MASK
- permission_fields_for_suffix()
- the existing special case in resolve_permission_bits_from_fields()
- behavior for unrelated permissions

Problem confirmed:
For NS_ID_PROJECT, token_candidates_for_namespace() currently returns "/{project_id}" first.
AdoGroup.ensure_group_permissions_from_fields() tries tokens sequentially and stops on HTTP 200/201/204.
Therefore "/{project_id}" can be accepted before the actual Project namespace token is tried.

For Manage Delivery Plans, allow_mask contains bit 33554432.

Make a targeted change in AdoGroup.ensure_group_permissions_from_fields() (or the smallest appropriate caller) so that:

if namespace_id == NS_ID_PROJECT
and (allow_mask & 33554432) != 0

then the FIRST token attempted is exactly:

$PROJECT:vstfs:///Classification/TeamProject/{project_id}

Keep the existing token candidates afterward as fallbacks.

Do NOT globally remove existing token candidates because other permissions are already working.

Also add temporary debug logging before each ACL write showing:
- namespace_id
- token
- descriptor
- allow_mask
- deny_mask
- response status

Do not change deny behavior yet.

Before applying the edit, show me:
1. exact file name
2. exact line numbers
3. current code
4. proposed replacement code
5. why the change affects Manage Delivery Plans without changing unrelated permissions.
