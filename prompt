Use the attached screenshots as the source of truth.

The Create Team page already has the correct layout. I want the Role Assignments and Review pages to use the EXACT SAME page shell.

DO NOT redesign the application.
DO NOT change business logic.
DO NOT change existing React components unless required for layout consistency.

========================================
GLOBAL PAGE LAYOUT
========================================

Every step (Create Team, Role Assignments, Review) must share the same page shell.

The page should consist of:

• Fixed left navigation
• Fixed top header
• Scrollable content area
• Fixed bottom action bar

========================================
LEFT SIDEBAR
========================================

- Fixed position
- Full viewport height
- Never scroll
- Same width on every page
- Preserve selected menu styling

========================================
TOP HEADER
========================================

- Fixed at top
- White background
- Full width excluding sidebar
- Never scroll
- Keep notification icon
- Keep Self Serve Home
- Keep profile icon

========================================
MAIN CONTENT
========================================

Only this area scrolls.

Each page should contain:

Page title

Subtitle

Stepper

Content Card

Spacing should remain identical across all steps.

========================================
ROLE ASSIGNMENTS PAGE
========================================

Keep the existing UI exactly as it is.

Do NOT move cards.

Do NOT redesign role groups.

Only improve layout consistency.

The Role Assignments page should scroll smoothly even with many role groups.

========================================
BOTTOM ACTION BAR
========================================

Every step must have the same sticky footer.

Height: 72px

White background

Border-top: #E5E7EB

Buttons aligned right.

Create Team page:
Back | Next

Role Assignments page:
Back | Next

Review page:
Back | Submit/Create

The footer must remain visible while scrolling.

========================================
SCROLLING
========================================

Use this layout:

App (100vh)
 ├── Fixed Sidebar
 ├── Fixed Header
 └── Main
      ├── Scrollable Content
      └── Sticky Footer

Only the content area should scroll.

Sidebar must never move.

Header must never move.

Footer buttons must never move.

========================================
IMPORTANT
========================================

Maintain the exact appearance of the current screenshots.

Only make the layout consistent across all pages.

Do not redesign.

Do not remove components.

Return the complete updated React components and styles.css.
