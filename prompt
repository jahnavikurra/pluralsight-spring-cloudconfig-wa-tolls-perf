Analyze this existing React UI repository and help me introduce a safe global styling foundation.

Important:
- This application is already in production.
- Do NOT change the visual appearance or behavior of existing pages.
- Do NOT refactor existing component CSS yet.
- Do NOT delete, rename, or move existing CSS files.
- Do NOT convert existing styles to CSS modules.
- Do NOT introduce Tailwind, Material UI, Bootstrap, styled-components, or another styling framework.
- Keep this change small and backward-compatible.

First inspect the repository and identify:
1. Whether this is Vite, CRA, or another React setup.
2. The application entry point such as main.tsx, index.tsx, App.tsx, etc.
3. All existing global CSS imports.
4. Whether fonts are currently defined anywhere.
5. Whether colors, spacing, typography, or CSS variables already exist.
6. Whether there are duplicate global/reset styles that we should be aware of.
7. Whether existing components rely on body/html/root styles that could be affected.

Before modifying anything, give me:
- Current styling structure
- Files involved
- Risks
- Exact files you propose to add/change

Then implement the following minimal structure:

src/
  styles/
    tokens.css
    global.css
    typography.css

If a styles folder already exists, use the existing structure instead of creating a duplicate.

Purpose of each file:

tokens.css
- Design tokens only.
- CSS custom properties under :root.
- Colors.
- Typography values.
- Font family.
- Font sizes.
- Font weights.
- Spacing.
- Border radius.
- Borders.
- Shadows.
- Z-index values if needed.
- Do NOT add component selectors such as .button, .card, .input, etc.

typography.css
- Only shared typography definitions if needed.
- Do not override existing component typography.
- Do not introduce a new external font yet.
- Prefer the font already used by the application.
- If no font is explicitly configured, use a safe enterprise font stack such as:
  "Segoe UI", Arial, sans-serif.

global.css
- Keep it intentionally minimal.
- Import tokens.css and typography.css if appropriate for the current build setup.
- Only add safe global rules such as:
  box-sizing
  body margin
  inherited font for form controls
- Do NOT introduce global button, input, card, table, heading, link, or layout styles that could alter existing screens.

Example starting tokens can be:

:root {
  --font-family-primary: "Segoe UI", Arial, sans-serif;

  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-md: 16px;
  --font-size-lg: 20px;
  --font-size-xl: 28px;

  --font-weight-regular: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  --color-primary: #0b4f8a;
  --color-primary-hover: #083d6c;

  --color-text-primary: #1f2937;
  --color-text-secondary: #667085;

  --color-background: #f7f9fc;
  --color-surface: #ffffff;

  --color-border: #d0d5dd;
  --color-border-light: #e4e7ec;

  --color-success: #169b62;
  --color-warning: #f79009;
  --color-error: #d92d20;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;

  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;

  --shadow-card: 0 1px 3px rgba(16, 24, 40, 0.08);
}

However:
- Before adding these values, compare them with existing UI colors and styling.
- Reuse existing values where possible.
- Do not blindly introduce colors that conflict with the current application.
- Tell me when a token value is an assumption.

Import global.css exactly once from the application entry point.

Do not import global.css separately into individual components.

Do not start using these tokens in existing pages yet.

After making the changes:
1. Show every file added.
2. Show every existing file modified.
3. Show the exact diff.
4. Explain why the changes will not affect existing screens.
5. Run the existing lint/build/type-check commands if available.
6. Report any errors without changing unrelated code.
7. Tell me how the new ADO License page should consume these variables.

Do not make any additional refactoring changes without asking me first.
