Keep the group.py token-order fix and the regression test.

Remove/undo all temporary changes in base.py.

Then run:
1. the targeted delivery plans regression test
2. all test_ado_group.py tests
3. all test_ado_utils.py tests

Do not make any additional production code changes.

Show me the final git diff. The only modified production file should be group.py, plus the regression test file test_ado_group.py.
