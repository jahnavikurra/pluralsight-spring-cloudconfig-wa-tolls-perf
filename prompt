Yes. Make the new styling foundation strictly opt-in with zero visual impact to existing pages.

Remove any global element selectors from the new styling foundation that affect existing elements, including input, button, textarea, select, headings, body, etc.
Do not modify existing application styles or Tailwind configuration.
Scope the new design tokens and styles under a wrapper class named .idp-scope rather than applying new styling globally through :root where practical.
Namespace all new CSS variables with --idp- to avoid collisions with existing theme.css, for example --idp-font-weight-medium, --idp-page-background, --idp-form-surface.
Keep global.css only as the import/entry point for tokens.css, typography.css, and components.css. Do not add selectors that change existing pages.
Keep all reusable new component classes namespaced with .idp-*.
Do not modify _app.tsx, existing src/pages routes, carousel files, Tailwind configuration, or existing page/component CSS.
After making the changes, show me the exact diff and confirm through a workspace-wide search that no existing page is using .idp-scope, .idp-*, or --idp-* yet.
