# Trace the final Azure DevOps ACL write

Do not change `NS_ID_DELIVERY_PLANS`, line 2389, or the existing Manage Delivery Plans special case.

We have confirmed that `Manage delivery plans` is handled earlier with:

```python
if field_key == normalize_perm_name(_S_MANAGE_DELIVERY_PLANS):
    allow_masks[NS_ID_PROJECT] = int(
        allow_masks.get(NS_ID_PROJECT, 0)
        | _MANAGE_DELIVERY_PLANS_ALLOW_MASK
    )
    continue
```

and `_MANAGE_DELIVERY_PLANS_ALLOW_MASK = 33554432`.

Now trace what happens AFTER `resolve_permission_bits_from_fields()`.

Please:

1. Find every caller of `resolve_permission_bits_from_fields()`.
2. Identify which caller is used during TeamAdmins onboarding.
3. Show where the returned `allow_masks` is iterated.
4. Show where `token_candidates_for_namespace()` is called.
5. Show the exact function that POSTs/PUTs the ACL to Azure DevOps.
6. For `NS_ID_PROJECT`, show the final:

   * namespace ID
   * token selected
   * group descriptor
   * allow value
   * deny value
   * request URL
   * JSON payload
7. Verify whether bit `33554432` is still present in `allow` immediately before the REST call.
8. Verify whether `33554432` is present in `deny`. If it is, identify where the deny bit must be cleared.
9. Verify whether the code stops after the first successful HTTP response even if that token does not correspond to the Project Permissions UI.
10. Do not modify any code yet.

At the end, tell me the exact function name and line numbers where the final ACL write happens, and identify the smallest likely fix for why Azure DevOps still shows:

`Manage delivery plans = Not set`

for the TeamAdmins group.
