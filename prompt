# Copilot Prompt — Trace Manage Delivery Plans Permission

I am debugging why Azure DevOps still shows **Manage delivery plans = Not set** for the TeamAdmins group.

In `utils.py`, the permission is already handled here:

```python
if field_key == normalize_perm_name(_S_MANAGE_DELIVERY_PLANS):
    allow_masks[NS_ID_PROJECT] = int(
        allow_masks.get(NS_ID_PROJECT, 0)
        | _MANAGE_DELIVERY_PLANS_ALLOW_MASK
    )
    continue
```

And:

```python
_MANAGE_DELIVERY_PLANS_ALLOW_MASK = 33554432
```

Do **not change this code yet**.

Please trace the code flow after `resolve_permission_bits_from_fields()` and find:

1. Every place where `resolve_permission_bits_from_fields()` is called.
2. Where the returned `allow_masks` is processed.
3. Where `token_candidates_for_namespace()` is called.
4. Where the Azure DevOps `accesscontrolentries` or `accesscontrollists` REST API is called.
5. What namespace ID, token, descriptor, allow mask, deny mask, and payload are actually sent for `NS_ID_PROJECT`.
6. Whether `33554432` is preserved in the final `allow` value or gets overwritten/filtered.
7. Whether an existing `deny` bit for `33554432` needs to be removed.
8. Which exact token is ultimately used for the Project namespace.
9. Whether the code considers an HTTP success on an incorrect token as successful and stops trying other tokens.

Please **do not make broad refactoring changes** and do not change existing permissions that are already working.

First show me the exact functions and line numbers involved in this flow:

`TeamAdmins → Manage delivery plans → allow_masks[NS_ID_PROJECT] → token → ACL payload → Azure DevOps API`

Then identify the most likely reason the UI remains `Not set`.

Finally, recommend the **smallest targeted code change** needed to make only `Manage delivery plans` become `Allow` without affecting the other existing ADO permissions.
