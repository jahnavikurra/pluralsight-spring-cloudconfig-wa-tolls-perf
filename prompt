Do not modify anything. Verify the current global styling setup after the reverts. Show me:

Which new styling files currently exist under src/styles.
The exact CSS import added to src/app/layout.tsx.
How tokens.css, global.css, and components.css are loaded from that import.
Confirm that existing pages are not consuming any of the new classes/tokens yet.
Confirm that the future ADO License page can use these tokens/classes without requiring changes to _app.tsx or the legacy src/pages routes.
