I need help fixing the **Azure DevOps "Manage delivery plans" permission** in this Python `utils.py` file.

Current issue:

* My automation is supposed to set **Project Settings → Permissions → Other → Manage delivery plans = Allow**.
* The code runs without an obvious error, but Azure DevOps still shows **Not set**.
* Please inspect the existing code carefully and make the **smallest possible change**. Do not refactor or change unrelated permission logic.

Important details:

```python
_S_MANAGE_DELIVERY_PLANS = "Manage delivery plans"

_MANAGE_DELIVERY_PLANS_ALLOW_MASK = 33554432
```

`33554432` is the project-level `AGILETOOLS_PLANS` permission.

The correct project security namespace should be:

```text
52d39943-cb85-4d7f-8fa8-c6baac873819
```

Currently I have logic similar to:

```python
matches = _find_matches_for_field(field, index)

if not matches:
    if field_key == normalize_perm_name(_S_MANAGE_DELIVERY_PLANS):
        allow_masks[NS_ID_PROJECT] = int(
            allow_masks.get(NS_ID_PROJECT, 0)
            | _MANAGE_DELIVERY_PLANS_ALLOW_MASK
        )
        continue

    missing.append(field)
    continue
```

I suspect this is wrong because the special handling for `"Manage delivery plans"` only executes when `not matches`.

Please do the following:

1. Trace this function end-to-end and confirm how `field`, `field_key`, `matches`, and `allow_masks` are processed.

2. Move/special-case `"Manage delivery plans"` **before** `_find_matches_for_field()` so it is always mapped to the Project namespace and doesn't accidentally match the Delivery Plan security namespace.

The intended logic should be approximately:

```python
field_key = normalize_perm_name(field)

if field_key == normalize_perm_name(_S_MANAGE_DELIVERY_PLANS):
    allow_masks[NS_ID_PROJECT] = (
        int(allow_masks.get(NS_ID_PROJECT, 0))
        | _MANAGE_DELIVERY_PLANS_ALLOW_MASK
    )
    continue

matches = _find_matches_for_field(field, index)

if not matches:
    missing.append(field)
    continue
```

3. Check the rest of `utils.py` to make sure `allow_masks[NS_ID_PROJECT]` is actually sent to Azure DevOps.

4. Verify that the code that applies the permission uses the correct **Project security namespace**:

```text
52d39943-cb85-4d7f-8fa8-c6baac873819
```

5. Verify that the permission is applied using the correct **project-level token**, based on the existing project's ID. It should correspond to:

```text
$PROJECT:vstfs:///Classification/TeamProject/{project_id}
```

6. Make sure the `33554432` bit is added to the existing allow mask using bitwise OR, without removing other project permissions:

```python
existing_allow | 33554432
```

7. Check whether the same permission bit exists in a deny mask. If the current code manages deny masks, make sure an existing explicit deny is not preventing Allow from becoming effective. Do not change unrelated deny permissions.

8. Do NOT change existing permission behavior for:

* Boards
* Repositories
* Pipelines
* Test Plans
* Analytics
* Other project permissions

9. Do NOT change function signatures, existing constants, API authentication, or unrelated namespace handling unless absolutely necessary.

10. Show me:

* the exact root cause,
* the exact lines that need to change,
* the updated code block,
* and explain the change in beginner-friendly terms.

Before editing, first trace where `allow_masks` is created and where it is finally sent to the Azure DevOps REST API. I want to make sure the problem is not only in the permission-matching section.
